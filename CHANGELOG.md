# CHANGELOG

## 與 v6.62 的刻意差異

移植的目標是忠實，不是照抄。活頁簿有十處自身的口徑不一致或防呆 hack——
在 Excel 裡它們不會報錯，但寫成程式後必須擇一，否則對帳永遠對不起來。

每一條都記錄了：發現的位置、原本的行為、現在的行為、以及對評等的實際影響。

---

### #1　營收年增率的「皆為正」在不同期別定義不同

**位置**　`六大財務指標評等!J5` vs `J15`、`J25`

最新一期的 AA 條件用 `COUNTIF(B5:G5,">=0")=6`（含 0），第二、三期卻用
`B15>0 ∧ C15>0 ∧ … ∧ G15>0`（嚴格大於 0）。

**現在**　統一為嚴格 `>0`，由 `config/rating_rules.toml` 的
`revenue_aa_positive` 控制。設成 `">=0"` 可重現原本第一期的行為。

**影響**　只在某月年增率**恰為 0** 時才有差別，實務上極少見；1,741 檔的
市場快照中沒有任何一筆受影響。

---

### #2　存貨周轉率的最新季與其餘季口徑不同

**位置**　`B9` vs `C9:G9`

最新季自行計算 `ISQ!B10 / (BSQ!B16 + BSQ!C16) × 2`（營業成本 ÷ 平均存貨），
其餘季直接取券商的 `FRQ!60`。兩者的年化方式與存貨平均基期未必一致。

**現在**　全部由三大報表自算（`transform/statements.inventory_turnover`），
不再讀券商值。三表是原始申報資料，可稽核，且比券商頁面回溯得更遠。

**影響**　數值可能有小數點後的差異，等第判定在 5439 的九期中完全一致。

---

### #3　稅後淨利年增率完全依賴 EPQ 工作表

**位置**　`B7`：`IF(設定!G2="Y", …, "---")`

若使用者把〔設定〕G2 關掉，整項回傳 `"---"`，六項平均隨之失效——一個
UI 開關可以讓整份評等靜默失真。

**現在**　由 `ISQ!98`（歸屬母公司淨利）直接計算年增率，移除開關。
`net_income` 序列同時吃三表與 EPQ，EPQ 只用來補三表回溯不到的更早季度。

---

### #4　自由現金流量的定義不是教科書定義

**位置**　`CFQ!59 + CFQ!75`

= 營業活動現金流量 **+ 全部投資活動現金流量**（含處分投資、收到利息股利），
而不是 `CFO − 資本支出`。

**現在**　**沿用原定義**。改了會位移所有歷史評等，而這項指標的價值正來自
它跨越多年的一致性。另外提供 `free_cash_flow(strict=True)` 計算
`CFO − |CapEx|` 供對照，但不參與評等。

---

### #5　年增率分母為 0 時用魔術數 0.3

**位置**　`營收!AH8`：`IF(D8=0, B8/0.3-1, E8)`

去年同期營收為 0 時，除以 0.3 以避開 `#DIV/0!`，產生一個巨大的假年增率。

**現在**　回傳 `None`，由評分函式視為缺值。假數值比缺值更危險：它會安靜地
把一檔沒有可比基期的個股推上 AA。

---

### #6　民國與西元混用

**位置**　`BASIC` 的上市日期是民國、`ISQ` 的期別是西元、`營收` 的月份是民國，
公式裡散落 `LEFT(x,3)+1911`。

**現在**　資料層一律用西元 `Quarter` 與 ROC 標籤兩種明確型別
（`calendar_tw.Quarter` / `RocMonth`），只在呈現層轉回民國。

---

### #7　營業利益率的 fallback 口徑未必一致

**位置**　`B6`：`IF(FRQ!B15="N/A", IF(EPQ!G7="", EPQ!F7/EPQ!N7*100, EPQ!G7*100), FRQ!B15)`

FRQ 缺值時退到 EPQ 的營益率，再退到 EPQ 的營業利益 ÷ 營業收入。三個來源的
分母定義未必相同。

**現在**　一律由 `ISQ!21 ÷ ISQ!8` 自算，不再 fallback。

---

### #8　「不評分」時型別不一致

**位置**　`評價簡表!B16`：`IF(六大!I9<>"", I9, H9)`

存貨周轉率不評分時 `I9` 是空字串，於是改抓 `H9` 的文字「不評分」——同一欄
有時是數字、有時是中文。

**現在**　`IndicatorResult` 分成 `status`（列舉）、`grade`（`Grade | None`）
與 `display`（字串）三個欄位。「不評分」不計入綜合評分的分母，
「數據不足」則使綜合評分無法計算——兩者語意明確分離。

---

### #9　1–2 月合併營收有三種不同寫法

**位置**　`B4` / `B14` / `B24`（以及 `B84` 的第四種巢狀寫法）

農曆年因素使 1、2 月營收必須合併觀察。活頁簿為此在每個期別區塊寫了不同的
判斷式，還在第八個區塊出現只處理「第二個合併月」的特例。

**現在**　抽成單一 `FinancialData.revenue_window(index, n)`：在合併月尚未
捲出領先窗格前走合併序列，之後走原始序列，永遠涵蓋六個真實月份。

**已知限制**　v6.62 用 `COUNTIF(...)=1` 判斷，當視野內同時出現**兩個**合併月
時會回退到錯誤的序列。本專案改用 `>= 1`，因此在跨兩個農曆年的長回溯上
與 v6.62 會有差異——這是修正，不是回歸。

---

### #10　互斥旗標並非真的互斥

**位置**　`J5` 與 `N5`

營收年增率的 AA（`J`）與 C（`N`）可以同時成立：當某月營收尚未公布時 `N` 觸發，
而其餘六個月的數字仍可能滿足 `J`。此時 `I = SUM(J:N)+4 = 5`，超出 0–4 量表；
`H` 欄則因為由 J 往 N 掃而顯示 AA。等第與分數互相矛盾。

**證據**　1,741 檔的市場快照中確實有一檔（6785）第一期的營收年增率評分是 `5`。

**現在**　「本月尚未公布營收」是一道**前置閘門**：命中即為 C，不再評估其他
條件。這符合規則文字「或最近一個月為負（排除年關因素）」的意圖，也消除了
不可能的分數。

**影響**　市場快照對帳 15,619 / 15,620——唯一不符的就是這一筆，引擎拒絕
給它一個不可能的分數。

---

## 版本

### 0.1.0

- 六大財務指標評等引擎，零相依，54/54 對帳通過
- 台股財報行事曆，15,669 組對應全部相符
- 由三大報表自算全部財務比率
- EPS 預估、本益比河流圖、PEG／總報酬、殖利率三價位
- TWSE / TPEx / MOPS / TDCC 官方來源（尚未在有網路環境實跑，見 README）
- MoneyDJ 券商鏡像備援，預設停用
- Jinja2 靜態網站：具投資價值清單、評等清單、統計、個股頁、規則說明
- GitHub Actions：ci / daily / monthly / quarterly
