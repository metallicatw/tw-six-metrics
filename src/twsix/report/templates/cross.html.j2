{# 舊網址的轉址頁。

   〔趨勢∩六大∩報酬〕已經併進〔趨勢×六大×報酬〕（原本那一頁叫〔台股趨勢選股〕）
   ——兩頁嵌的本來就是同一份報告，差別只有兩個輸入框的預設值（理由寫在
   trend.html.j2 裡）。

   這一頁留著，是因為它曾經是一個可以分享、可以加書籤的網址。把檔案拿掉的話，
   那些連結會變成 GitHub Pages 的 404——一個「這個網站壞了」的畫面，而實際上
   內容好好地在隔壁。

   `http-equiv=refresh` 而不是 JS：轉址不該取決於 JavaScript 有沒有跑起來。
   `rel=canonical` 讓搜尋引擎把權重併過去。底下那一行字是給「meta refresh 被
   瀏覽器設定擋掉」的人看的——它不是裝飾，那是那些人唯一的出路。 #}
<!DOCTYPE html>
<html lang="zh-Hant">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<meta http-equiv="refresh" content="0; url={{ target }}">
<link rel="canonical" href="{{ target }}">
<title>已移至〔趨勢×六大×報酬〕｜{{ site_title }}</title>
<style>
  body{font-family:system-ui,-apple-system,"Noto Sans TC",sans-serif;
       background:#0d1117;color:#c9d1d9;margin:0;
       display:flex;align-items:center;justify-content:center;
       min-height:100vh;padding:24px;text-align:center;line-height:1.8}
  a{color:#58a6ff}
</style>
</head>
<body>
<p>〔趨勢∩六大∩報酬〕已經併進〔趨勢×六大×報酬〕。<br>
沒有自動跳轉的話，請按這裡：<a href="{{ target }}">{{ target }}</a></p>
</body>
</html>
