<!doctype html>
<html lang="zh-Hant">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>{% block title %}{{ site_title }}{% endblock %}</title>
{% if assets %}<link rel="stylesheet" href="{{ rel }}assets/site.css?v={{ asset_v }}">
{% else %}<style>{{ site_css }}</style>{% endif %}
</head>
{# 個股頁上，頁首那顆按鈕預設就指這一檔——讀者不必再把代號打一次。已經有完整
   報告的也給（字改成「重新抓取」）：資料會過期，而且新增的區塊只能靠重抓補上。 #}
<body{% if grab_code %} data-grab="{{ grab_code }}"{% endif %}{% if grab_full %} data-full="1"{% endif %}>
<header class="top"><div class="in">
  {# 檢視模式的切換放在頁首右上角。它是這一頁最上面的一個動作，而它原本夾在
     頁尾那行小字裡——那裡是「這份網站什麼時候建的」，一個資訊的位置，不是一個
     按鈕該待的地方。

     手機上這個站預設就是窄版，但表格很寬，有時候讀者寧可縮小字捲著看完整的
     欄位；在桌機上反過來，有人想預覽窄版長什麼樣。兩邊各有一個理由，所以給
     一顆按鈕而不是替他決定。選擇存在這台瀏覽器裡。 #}
  <button type="button" id="viewmode" class="viewmode" aria-pressed="false">切換手機版</button>
  {# 建站時間跟在站名後面，同一行。
     它原本自己佔一整行放在頁首最下面——那一行的內容是「這份網站什麼時候建的」，
     一件**只有懷疑資料舊了的時候才會去看**的事，卻拿到了和導覽列同一等級的
     垂直空間。跟在站名後面、字級降到 11px，要找的時候找得到，不找的時候不佔
     一行。 #}
  <h1><a href="{{ rel }}index.html">{{ site_title }}</a><span class="built">{{ generated_at }}</span></h1>
  {# 三個分頁暫時不連：它們讀的是一年前的全市場快照，而排名和分布用舊資料算出來
     不只是過時，是理直氣壯地過時。個股頁不同，那是現抓的。頁面照樣產生，直接
     開網址還是看得到，只是導覽不指過去。#}
  {# 順序是由近而遠：〔評等清單〕是全市場的體質，〔觀察清單〕是你從裡面挑出來
     的那幾檔，〔趨勢選股〕是今天技術面在動的那幾檔，〔市場監控〕是這一切之外
     的大盤與海外。前兩項講「這些公司是什麼」，後兩項講「現在在發生什麼」。 #}
  {# 名字裡帶著範圍。原本四項叫〔評等清單〕〔觀察清單〕〔趨勢選股〕〔市場監控〕，
     四個字一組、長得一樣，看不出前三項全是台股、第四項根本不是。

     顏色（見 site.css 的 .nav-*）補的是另一半：現在四個名字都以「台股」或「全球」
     開頭，前兩個字相同，眼睛要讀到第三個字才分得出來——顏色讓「我在哪一頁」在
     讀完字之前就成立。

     ── 為什麼是一列可以橫滑的按鈕 ──────────────────────────

     原本是會換行的文字連結。四個名字各五到十個字，手機上換成兩、三行，而那幾行
     長度不一，「現在在哪一頁」要在一團參差的字裡找底線。

     改成等高的按鈕排一列、溢出時橫向捲：一列就是一列，不管視窗多窄都不會變形，
     而目前所在的那一顆用實心底色，離一公尺遠也看得出來。橫捲條刻意留著
     （見 site.css 的 .nav），因為看不見的捲動等於沒有捲動。 #}
  <nav>
    <a class="nav-list" href="{{ rel }}index.html"{% if page=='list' %} aria-current="page"{% endif %}>台股評等清單</a>
    <a class="nav-watch" href="{{ rel }}watchlist.html"{% if page=='watchlist' %} aria-current="page"{% endif %}>台股觀察清單</a>
    {# 這兩項只有那個檔案真的在 site/ 裡才出現。它們由建站流程從另外兩個 repo
       複製進來，取不到的時候少一個導覽項，比多一個 404 的連結好。 #}
    {% if trend_page %}
    <a class="nav-trend" href="{{ rel }}{{ trend_page }}"{% if page=='trend' %} aria-current="page"{% endif %}>台股趨勢選股</a>
    {% endif %}
    {% if monitor_page %}
    <a class="nav-mon" href="{{ rel }}{{ monitor_page }}"{% if page=='monitor' %} aria-current="page"{% endif %}>全球市場監控＋日股觀察</a>
    {% endif %}
    {% if 'picks' not in hidden_pages %}
    <a class="nav-more" href="{{ rel }}picks.html"{% if page=='picks' %} aria-current="page"{% endif %}>具投資價值</a>
    {% endif %}
    {% if 'stats' not in hidden_pages %}
    <a class="nav-more" href="{{ rel }}stats.html"{% if page=='stats' %} aria-current="page"{% endif %}>評等統計</a>
    {% endif %}
    {% if 'about' not in hidden_pages %}
    <a class="nav-more" href="{{ rel }}about.html"{% if page=='about' %} aria-current="page"{% endif %}>評分規則</a>
    {% endif %}
  </nav>
  {# 頁首這一塊（搜尋框 ＋ 抓取）只出現在〔台股評等清單〕與個股頁。
     它在〔趨勢選股〕〔全球市場監控〕那兩頁完全沒有作用——那裡是別的 repo 產的
     報告嵌進來的 iframe，輸入代號按抓取跟那一頁講的事情沒有關係；而〔觀察清單〕
     的篩選框就在下面一行，頁首再放一個輸入框只是兩個框互相搶。

     個股頁保留，是因為那顆按鈕在那裡指的是**這一檔**（見 body 的 data-grab），
     而那是唯一能把一檔資料換成最新的入口。拿掉它等於那一頁只能讀、不能更新。

     site.js 的搜尋／抓取整個模組第一行就是 `if(!box) return;`——沒有這個表單的
     頁面直接跳過，不必再加任何判斷。 #}
  {% if page in ('list', 'stock') %}
  <form class="find" role="search" action="{{ rel }}index.html" method="get">
    {# The form's action is the fallback, and it is a real one: with scripting
       off, submitting takes the reader to 評等清單, which has its own filter
       and lists every stock.  The combobox below is an enhancement on top of
       a page that already worked. #}
    <div class="findrow">
      <input id="find" name="q" type="search" autocomplete="off" spellcheck="false"
             placeholder="輸入代號或名稱…　（按 / 跳到這裡）" aria-label="搜尋個股"
             role="combobox" aria-expanded="false" aria-controls="find-list"
             aria-autocomplete="list">
      {# 只有在「這一檔還沒有完整報告，而且抓得到」的時候才出現。沒東西可抓時
         留一顆按不動的按鈕，比沒有按鈕更難懂。 #}
      <button type="button" id="grabnow" class="btn-grab" hidden>抓取</button>
      {# 權杖的設定就在「抓取」旁邊，因為那是它唯一會被想到的時刻——按下抓取、
         發現要權杖。它原本在頁首另一行，離那顆按鈕三十幾個像素遠，而且那一行
         平常是空的。 #}
      <button type="button" id="tokenlink" class="linkish tokenlink" hidden>設定抓取權杖</button>
    </div>
    <ul id="find-list" role="listbox" aria-label="搜尋結果" hidden></ul>
    {# 進度面板要跟搜尋框對齊，所以必須待在 .find 裡面——它是 position:relative
       的那一層。放在外面會拿 header 當定位基準，掉到畫面上量不到的地方去。
       只有在 `twsix serve` 底下才會用到：載入時問一次 /api/ping，GitHub Pages
       上問不到就永遠不顯示。同一份 HTML，兩種行為。 #}
    <div id="grab" class="grab" hidden>
      {# 面板蓋在搜尋結果上面。跑完會自己換頁，但失敗或逾時的時候它會一直留著，
         把搜尋框整個擋住——所以要給一個關得掉的辦法。 #}
      <button type="button" class="x" id="grab-x" aria-label="關閉進度">✕</button>
      {# 三層：這在做什麼（含已經等了多久）、現在跑到哪一段、一條分段進度條。
         條子本身 aria-hidden——它講的事情 .stage 那一行已經用文字講過一次了，
         螢幕閱讀器不需要聽到十四個方塊。 #}
      <p class="head"></p>
      <p class="stage" aria-live="polite"></p>
      <div class="bar" aria-hidden="true"></div>
      {# 每一步第幾秒。預設收起來——平常看的人只要知道「還在跑、跑到哪」，
         但這段等待橫跨三個我們管不到的東西（GitHub 排隊、workflow、Pages CDN），
         而「總共兩分鐘」沒辦法告訴任何人該修哪一段。出事的時候自動展開。 #}
      <details class="detail">
        <summary>詳細</summary>
        <pre class="log" aria-live="polite"></pre>
      </details>
    </div>
  </form>
  {% endif %}
  {# 這一行原本擠了四件事：產生時間、檔數、財報季別、營收月份。後面三件在別的
     地方各自說過一次了——檔數與季別在〔評等清單〕的第一句，落後多久在下面那條
     ⚠ 橫幅——所以留在這裡只是把最上面那一行讀成一串參數。

     換成一件事：這份網站是什麼時候建的。

     後面原本還有一句「個股資料請記得更新」。它在只有 183 檔抓過報表的時候是實話，
     現在不是了：全部 1,877 檔都抓過，收盤價、三大法人與個股新聞每天由排程自己
     更新，而每一列的日期就寫在清單上。一句每一頁都出現、卻已經不成立的提醒，
     讀起來像網站在推卸責任。 #}
</div></header>
<div class="wrap">
{% if data_age_note %}
<p class="stale" role="status">⚠　{{ data_age_note }}　頁面上的評等與估值都是依這個時點計算的。</p>
{% endif %}
{% block body %}{% endblock %}
<footer>
  由公開資料自動產生，僅供研究參考，不構成投資建議。
</footer>
</div>
{# 個股頁十個分頁，每個分頁自己就有好幾螢幕；捲到底想換分頁，得一路捲回去。
   一顆固定在右下角的按鈕解決這件事，而且只在真的捲下去之後才出現——沒捲的時候
   它只是擋住內容。 #}
<button type="button" id="totop" hidden aria-label="回到頁面最上方" title="回到最上方">↑</button>
<script>window.TWSIX={rel:{{ rel|tojson }},repo:{{ (repo or '')|tojson }},built:{{ (build_id or '')|tojson }}};</script>
{% if assets %}<script src="{{ rel }}assets/site.js?v={{ asset_v }}" defer></script>
{% else %}<script>{{ site_js }}</script>{% endif %}
</body>
</html>
