{% extends "base.html.j2" %}
{% from "_macros.html.j2" import tip %}
{% block title %}趨勢X六大X報酬｜{{ site_title }}{% endblock %}
{% block body %}
{# 和〔市場監控〕同一套做法：報告本身是另一個 repo 每個交易日重新產生的一整份
   HTML（自己的 CSS、自己的版面、自己的 plotly），這裡用 iframe 把它嵌進來。

   * 頁首、導覽列、搜尋框全都在，讀者從頭到尾沒有離開這個網站。
   * 報告一個位元組都沒有被動到。它每天重新產生，改它遲早會壞。

   同源（就在這個網站的 /trend-report.html），所以沒有 X-Frame-Options 或
   CORS 的問題。

   ## 為什麼只剩一頁

   這裡本來有兩頁：〔台股趨勢選股〕（技術面四關）和〔趨勢∩六大∩報酬〕（四關
   再加兩個門檻）。兩頁嵌的是**同一份報告**，差別只有網址後面那個 `#cross`
   ——它讓報告把兩個門檻預先填成「六大 > 3」「報酬風險比 > 2」。

   而那兩個門檻就攤在報告最上面那一列，隨時可以改成 0（＝那一關不啟用）。
   也就是說，兩頁之間唯一的差別是**兩個輸入框的預設值**，而讀者在任何一頁都
   一秒就能切到另一邊。兩個導覽項、兩個網址、兩份說明，買到的只有那個預設值。

   所以併成一頁，預設帶 `#cross`：一進來是最嚴的那一組，想看寬的把兩個數字
   改成 0。舊網址 `cross.html` 留一頁轉址（見 build.py 的 CROSS_PAGE），
   已經分享出去的連結不會變成 404。 #}
{# 〔預設篩選條件〕：原本是報告裡另一顆燈泡＋彈出視窗，2026-09-23 併進這一顆，
   報告上那一顆拿掉。內容由建站時從報告的 <template id="tf-rules"> 抽出來
   （build.py 的 trend_rules_html）——門檻數字只有 tw-trend-filter 知道，這裡
   不另寫一份。抽不到的時候退回下面那段不帶數字的說明。 #}
<h2>趨勢X六大X報酬　{% call tip() %}
  <b>技術面四關全過，再加兩個基本面的條件。</b>全市場約 1,900 檔先過這四關：
  {% if rules_html %}
  <span class="trend-rules" role="group" aria-label="預設篩選條件">
    <b class="trh"><span aria-hidden="true">💡</span> 預設篩選條件</b>
    {{ rules_html }}
  </span>
  {% else %}
  <b>趨勢方向</b>（收盤 &gt; 60MA 且 20MA &gt; 60MA）
  →　<b>動能啟動</b>（10 日內黃金交叉或布林壓縮 ≤ 12%）
  →　<b>突破確認</b>（突破布林上軌或 20 日高點）
  →　<b>量能背書</b>（量比 ≥ 1.2 倍）。停損取 3×ATR(14)。
  <br><br>
  {% endif %}
  過了之後，每一檔卡片上還會帶著兩個基本面的數字：<b>六大財務指標最新綜合
  評分</b>（滿分約 3.83）與 <b>報酬風險比</b>（〔EPS預估與估價〕算的）。
  <br><br>
  <b>這兩個數字預設「只顯示、不篩」</b>——上面那一列的〔六大評分〕〔報酬風險比〕
  預設都是 <b>0</b>，也就是那一關不啟用。想把它們變成條件，把數字打上去就好，
  畫面會當場重篩：常用的一組是<b>六大 &gt; 3</b>（大約是前 10%）與
  <b>報酬風險比 &gt; 2</b>（「報酬風險 &gt; 2 才有買進的意義」）。
  <br><br>
  為什麼預設不啟用：那一組很嚴。實測 2026/09/18，25 檔趨勢入選裡只有 1 檔同時
  過另外兩關；而四關本身在弱勢的日子就可能是零檔。兩個嚴格條件疊起來，打開這
  一頁最常看到的是一張空名單——而空名單沒辦法讓人判斷「今天是沒有標的，還是
  門檻太緊」。
  <br><br>
  <b>報酬風險比是用今天的收盤算的。</b>目標價與下檔價和股價無關（＝歷年本益比
  × 預估EPS），由這個網站每天發布；報酬風險比的最後一步要股價，而那一步在
  趨勢那支程式裡算——它手上就有今天的收盤。兩邊因此是同一天的同一個價格。
  <br><br>
  <b>無風險</b> 是股價已經低於下檔價：沒有下檔風險，比 3.0 還好，一定入選。
  四關過了但<b>算不出</b>報酬風險比的（多半是還沒回補到〔年度交易資訊〕，
  沒有本益比區間）不會消失，它們列在名單下方並標示原因——
  「消失」和「不合格」在畫面上長得一模一樣，而它們不是同一件事。
  <br><br>

  名單上每一檔都可以直接跳到<b>這個網站的個股頁</b>看它的六大財務指標評等
  ——技術面挑出來之後，下一個問題是這家公司的體質。
  <br><br>
  由 <a href="https://github.com/metallicatw/tw-trend-filter" rel="noopener">tw-trend-filter</a>
  每個交易日台北 15:10 產生，這裡嵌的是同一份。
  <a href="{{ rel }}{{ report }}#cross" target="_blank" rel="noopener">單獨開啟這份報告 ↗</a>
{% endcall %}</h2>
{# `.tall`：桌機上 iframe 幾乎一整個視窗高（見 site.css）。78vh 的時候圖被
   壓到約 4:1，使用者說「壓太扁平了，看不出變化」。 #}
<iframe id="trend-frame" class="embed tall" src="{{ rel }}{{ report }}#cross"
        title="趨勢X六大X報酬：技術面四關全過，再篩六大評分與報酬風險比"></iframe>
{% endblock %}
