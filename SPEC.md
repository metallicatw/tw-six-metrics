# 六大財務指標評等 — 演算法規格書

> 逆向來源：`5439_六大財務指標評等v6.62pub_20250913.xlsm`
> 工作表〔六大財務指標評等〕（codeName 工作表28 / `xl/worksheets/sheet8.xml`）
> 文字規則原文位於同表 `A117:B176`。所有內容引自原檔公式，未經推測。

---

## 1. 表格結構

九個相同的區塊，每塊 10 列：

| 區塊 | 列範圍 | 綜合評分 | 具投資價值 |
|---|---|---|---|
| 1（最新） | 3–12 | `I11` | `I12` |
| 2 | 13–22 | `I21` | `I22` |
| 3 | 23–32 | `I31` | `I32` |
| … | … | … | … |
| 9 | 83–92 | `I91` | `I92` |

每個區塊內：

```
列 +0   稅後淨利率（B:G）          ← 供「稅後淨利年增率」的正淨利率閘門使用
列 +1   營收月份（B:G）            民國年月，1-2 月合併為 "115/01-02"
列 +2   ① 營收年增率（B:G，%）      H 等第  I 分數  J:N 旗標  O 不評分
列 +3   ② 營業利益率（B:G，%）
列 +4   ③ 稅後淨利年增率（B:G，%）
列 +5   ④ 每股盈餘 EPS（B:G，元）
列 +6   ⑤ 存貨周轉率（B:G，次）
列 +7   ⑥ 自由現金流量（B:G，佰萬）
列 +8   財報季度（B:G）            "2026.2Q" 格式
列 +9   區塊序號 / 具投資價值
```

B 欄為最新一期，G 欄為第六期（往回遞減）。

---

## 2. 評分機制

```
J..N 五個旗標各自判定；命中回傳自己的分數，未命中回傳 -1
    J → 4 (AA)   K → 3 (A)   L → 2 (BB)   M → 1 (B)   N → 0 (C)

I（分數） = IF(O = 0, SUM(J:N) + 4, "")
H（等第） = 由 J 往 N 掃，第一個 ≠ -1 者對應的字母；全部 -1 → "不評分"
O（不評分）= IF(AND(J=-1,K=-1,L=-1,M=-1,N=-1), 1, 0)
```

`SUM(J:N)+4` 成立的前提是恰有一個旗標命中：`x + (-1)×4 + 4 = x`。

---

## 3. 六個指標的判定規則

以下 `AVG` 皆為 `AVERAGE`，`CNT(range, cond)` 為 `COUNTIF`。
`B..G` 指同列的六個期別欄，`B` 為最新期。

### ① 營收年增率（近 6 個月）

| 旗標 | 條件 |
|---|---|
| J = 4 (AA) | `CNT(B:G, ">=0") = 6 ∧ AVG(B:G) >= 25 ∧ B >= C` |
| K = 3 (A) | `(CNT(B:G,">=0")=6 ∧ 10 <= AVG(B:G) < 25 ∧ B >= C)` ∨ `(CNT(B:G,">=0")=6 ∧ AVG(B:G) >= 25 ∧ B < C ∧ B >= C*0.5 ∧ M = -1)` |
| L = 2 (BB) | `(CNT(B:G,"<0") >= 1 ∧ N = -1 ∧ M = -1)` ∨ `(J=-1 ∧ K=-1 ∧ M=-1 ∧ N=-1)` |
| M = 1 (B) | `AVG(B:G) >= 0 ∧ B < C ∧ C < D ∧ D < E ∧ N = -1` |
| N = 0 (C) | `AVG(B:G) < 0 ∨ B < 0 ∨ 該月營收儲存格為空` |

**已知不一致**：最新期（列 5）用 `CNT(B:G,">=0")=6`，第 2、3 期（列 15、25）改用
`B>0 ∧ C>0 ∧ … ∧ G>0`（嚴格大於 0）。移植時須擇一。

資料來源：`營收!AH` 欄年增率，以 `營收!AD/AG` 欄「年/月」鍵 `VLOOKUP` 取得，再 `×100` 並 `ROUND(...,1)`。

### ② 營業利益率（近 4 季）

定義 `STABLE ≔ B >= C*0.8 ∧ C >= D*0.8 ∧ D >= E*0.8`（季對季跌幅在 20% 以內）

| 旗標 | 條件 |
|---|---|
| J = 4 | `STABLE ∧ AVG(B:E) >= 15` ∨ `STABLE ∧ 10 <= AVG(B:E) < 15 ∧ B > C` |
| K = 3 | `STABLE ∧ 10 <= AVG(B:E) < 15 ∧ B <= C` ∨ `STABLE ∧ 5 <= AVG(B:E) < 10 ∧ B > C` |
| L = 2 | `((C < D*0.8 ∨ D < E*0.8) ∧ ¬(B < C*0.8) ∧ AVG(B:E) >= 5)` ∨ `(J=-1 ∧ K=-1 ∧ M=-1 ∧ N=-1)` |
| M = 1 | `(B < C*0.8 ∨ (0 <= AVG(B:E) < 5)) ∧ N = -1` |
| N = 0 | `AVG(B:E) < 0 ∨ B < 0` |

資料來源：`FRQ!15`；若為 `"N/A"` 則 fallback `EPQ!G*100`，若 `EPQ!G` 為空則 `EPQ!F / EPQ!N * 100`。

### ③ 稅後淨利年增率（近 4 季）

**外層閘門**：若同區塊 `$L$3 = "Y"`（正淨利率判斷開啟），
所有旗標外加條件 `CNT(稅後淨利率 B:D, ">=0") = 3`，不滿足時 J..M 皆 -1、N = 0。

| 旗標 | 條件（閘門通過後） |
|---|---|
| J = 4 | `(B>=0 ∧ C>=0 ∧ D>=0 ∧ B>C)` ∨ `(B>=50 ∧ C>=50 ∧ D>=50)` |
| K = 3 | `B>=0 ∧ C>=0 ∧ B >= C*0.5 ∧ CNT(D:E,"<0") < 2 ∧ J=-1 ∧ M=-1` |
| L = 2 | `(B>=0 ∧ C>=0 ∧ B < C*0.5 ∧ CNT(D:E,"<0")<2 ∧ J=-1 ∧ M=-1)` ∨ `(B>=0 ∧ C<0 ∧ CNT(C:E,"<0")<2)` |
| M = 1 | `(B<0 ∨ CNT(B:E,"<0")>=2 ∨ (B<C ∧ C<D ∧ B<50 ∧ B>=0)) ∧ N=-1` |
| N = 0 | `B < 0 ∧ C < 0` |

資料來源（最新期，`B7`）：
```
IF(設定!G2 = "Y",
   IF(民國季度(B11) = EPQ!A7,
      ROUND((EPQ!L7 - EPQ!L11) / ABS(EPQ!L11) * 100, 1),   ' L = 歸屬母公司淨利，+4 列 = 去年同季
      "N/A"),
   "---")
```
其餘期以 `INDIRECT`/`ADDRESS` 動態定位到 EPQ 對應列的第 12 欄（L）。
**若〔設定〕G2 ≠ "Y"，整項回傳 `"---"`，綜合評分失效。**

另有稽核用的 A/B 對照（Q5:W7 區）：
- A = 合併稅後淨利年增率 = `(ISQ!B76 - ISQ!F76) / ABS(ISQ!F76) * 100`，其餘期取 `FRQ!41`
- B = 母公司稅後淨利年增率 = 上述 `B7`
- `B/A(%)` = `IF(A>0 ∧ B>0, B/A, "N/A")`

### ④ 每股盈餘 EPS（近 4 季累計）

| 旗標 | 條件 |
|---|---|
| J = 4 | `SUM(B:E) >= 5 ∧ B >= 0` |
| K = 3 | `3 <= SUM(B:E) < 5 ∧ B >= 0` |
| L = 2 | `1 <= SUM(B:E) < 3 ∧ B >= 0` |
| M = 1 | `(0 <= SUM(B:E) < 1)` ∨ `(B < 0 ∧ N = -1)` |
| N = 0 | `SUM(B:E) < 0` |

資料來源：最新期 `EPQ!K7`，其餘期 `FRQ!31`。

### ⑤ 存貨周轉率（近 4 季）

**不評分閘門**（優先於所有旗標）：
```
PRODUCT(B:E) <= 0  ∨  BSQ!$K$7 <= 4%  ∨  BSQ!$L$7 <= 1%
```
其中 `BSQ!K7` = 季度存貨/營收 = `ROUND(BSQ!B16 / ISQ!B8, 2)`，
`BSQ!L7` = 年度存貨/營收（依最新季別取對應四季營收合計）。
命中時 J..N 全部 -1 → `O = 1` → 該項不評分，不計入綜合評分平均。

| 旗標 | 條件（閘門未命中時） |
|---|---|
| J = 4 | `STABLE ∧ AVG(B:E) >= 1.5 ∧ L = -1` |
| K = 3 | `STABLE ∧ AVG(B:E) < 1.5 ∧ L = -1` |
| L = 2 | `(B<C ∧ C<D ∧ ABS((B-D)/D) > 20%)` ∨ `(C<D ∧ D<E ∧ ABS((C-E)/E) > 20%)`，且 `M=-1 ∧ N=-1`；整式包 `IFERROR(...,-1)` |
| M = 1 | `(B < C*0.8 ∨ C < D*0.8 ∨ D < E*0.8) ∧ N = -1` |
| N = 0 | `B < C*0.8` |

（`STABLE` 定義同 ②）

資料來源：最新期自算 `IF(AND(BSQ!B16, BSQ!C16), ISQ!B10 / (BSQ!B16 + BSQ!C16) * 2, 0)`
＝ 營業成本 ÷ 平均存貨 × 2；其餘期取 `FRQ!60`。**兩種口徑不同，移植時須統一。**

### ⑥ 自由現金流量（近 6 季）

| 旗標 | 條件 |
|---|---|
| J = 4 | `B>=0 ∧ C>=0 ∧ D>=0 ∧ E>=0 ∧ F>=0 ∧ G>=0` |
| K = 3 | `SUM(B:G) >= 0 ∧ SUM(B:E) >= 0 ∧ J = -1` |
| L = 2 | `SUM(B:G) < 0 ∧ SUM(B:E) >= 0` |
| M = 1 | `SUM(B:G) >= 0 ∧ SUM(B:E) < 0` |
| N = 0 | `SUM(B:G) < 0 ∧ SUM(B:E) < 0` |

資料來源：`CFQ!59 + CFQ!75`
＝ **來自營運之現金流量（CFO） + 投資活動之現金流量（CFI 全額）**。
注意這不是教科書的 `CFO − CapEx`，移植時務必沿用原定義以維持歷史一致性。

---

## 4. 綜合評分與投資價值判定

```
綜合評分 I11 = IFERROR(
    IF(AND($E$1 = "", CNT(I5:I10, "N/A") = 0), AVERAGE(I5:I10), "N/A"),
    "N/A")
```
- `$E$1` 為排除旗標：`IF(LEFT(ISQ!A5,2)="查無", "查無資料", IF(ISQ!A21<>"營業利益", "金融保險業不適用", ""))`
- 任一指標為 `"N/A"` → 整體 `"N/A"`
- 「不評分」的指標（I 欄為空字串）自動被 `AVERAGE` 略過，分母變小

```
具投資價值 I12 = IFERROR(
    IF(AND(CNT(I5:I10,"=0") = 0,
           CNT(I5:I10,"=1") = 0,
           I11 >= 3,
           I11 - I21 > -0.3),
       "具投資價值", ""),
    "")
```
即：六項無 0 分、無 1 分、本期綜合評分 ≥ 3、且較上期未下滑超過 0.3。

---

## 5. 原始資料欄位對照

| 用途 | 位置 | 科目名稱 |
|---|---|---|
| 稅後淨利率（閘門） | `FRQ!17` | 稅後淨利率 |
| 營業利益率 | `FRQ!15` | 營業利益率 |
| 每股盈餘 | `FRQ!31` / `EPQ!K` | 每股盈餘 |
| 稅後淨利成長率（合併 A） | `FRQ!41` / `ISQ!76` | 稅後淨利成長率／合併總損益 |
| 稅後淨利年增率（母公司 B） | `EPQ!L` / `ISQ!98` | 歸屬母公司淨利（損） |
| 存貨周轉率 | `FRQ!60` / `ISQ!10 ÷ BSQ!16` | 存貨週轉率(次)／營業成本、存貨 |
| 自由現金流量 | `CFQ!59 + CFQ!75` | 來自營運之現金流量 + 投資活動之現金流量 |
| 低庫存排除門檻 | `BSQ!K7`, `BSQ!L7` | 季度存貨/營收、年度存貨/營收 |
| 加權平均股數 | `ISQ!105` | 加權平均股數 |
| 營業收入 | `ISQ!8` | 營業收入淨額 |
| 營業成本 | `ISQ!10` | 營業成本 |
| 營業利益 | `ISQ!21` | 營業利益 |
| 存貨 | `BSQ!16` | 存貨 |

---

## 6. 台股財報行事曆（`營收!I21:L37`）

「當某月營收公布時，最新可得的季報是哪一季」的對照表。同一營收月份出現兩列，
代表該月存在跨季度財報（例如 8/10 發 7 月營收時仍是 1Q，8/14 公布 2Q 後才轉 2Q）。

| 營收月份 | 發布日 | 最新季報 | 年度位移 |
|---|---|---|---|
| 01 | 02/10 | 3Q | −1 |
| 02 | 03/10 | 3Q | −1 |
| 02 | 03/31 | 4Q | −1 |
| 03 | 04/10 | 4Q | −1 |
| 04 | 05/10 | 4Q | −1 |
| 04 | 05/15 | 1Q | 0 |
| 05 | 06/10 | 1Q | 0 |
| 06 | 07/10 | 1Q | 0 |
| 07 | 08/10 | 1Q | 0 |
| 07 | 08/14 | 2Q | 0 |
| 08 | 09/10 | 2Q | 0 |
| 09 | 10/10 | 2Q | 0 |
| 10 | 11/10 | 2Q → 3Q | 0 |
| 11 | 12/10 | 3Q | 0 |
| 12 | 01/10 | 3Q | 0 |

---

## 7. 待決議清單（Excel 自身的口徑不一致）

1. 營收年增率 AA 條件：最新期 `>=0`，第 2/3 期 `>0` → 擇一
2. 存貨周轉率：最新季自算單季 vs 其餘季取券商值 → 統一為自算
3. 稅後淨利年增率完全依賴 EPQ（設定 G2） → 改由 ISQ!98 自算
4. FCF = CFO + 全部 CFI，非 CFO − CapEx → 沿用原定義，另加 `fcf_strict` 對照欄
5. 年增率分母為 0 時用 `B/0.3 − 1` 的魔術數 → 改回傳 None
6. 民國／西元混用 → 資料層一律 ISO，呈現層再轉民國
7. 營業利益率的 EPQ fallback 定義未必一致 → 改由 ISQ!21 ÷ ISQ!8 自算
8. 「不評分」時〔評價簡表〕改抓 H 欄文字，型別不一致 → 分成 `score: Optional[int]` 與 `grade: str`
9. 1–2 月合併營收在三個期別區塊有三種寫法 → 抽成單一 `resolve_revenue_month()`
